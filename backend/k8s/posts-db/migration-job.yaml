apiVersion: batch/v1
kind: Job
metadata:
  name: posts-migrate
  namespace: posts
  labels:
    stage: migrationNA
spec:
  backoffLimit: 0
  template:
    spec:
      restartPolicy: Never
      containers:
        - name: migrate
          image: social-network/posts:dev
          imagePullPolicy: Never
          env:
            - name: DATABASE_URL
              valueFrom:
                secretKeyRef:
                  name: posts-secret
                  key: DATABASE_URL
          command: ["sh", "-c"]
          args:
            - |
              ./migrate && \
              echo "Running seeds..." && \
              ./seeds/seed.sh
