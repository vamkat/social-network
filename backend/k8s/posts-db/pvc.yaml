apiVersion: v1
kind: PersistentVolumeClaim
metadata:
  name: posts-db-pvc
  namespace: posts
  labels:
    stage: pvc
spec:
  accessModes:
    - ReadWriteOnce
  resources:
    requests:
      storage: 1Gi
