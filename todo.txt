--check how can we cache rarely used/updated data => FETCH API REVALIDATE

TODOS:
1. Pagination feature for posts and comments (feed/public, feed/friends, profile/[id], groups/[id])

2. Mock data for comments (EXAMPLE IN src/app/mock-data/comments.js). Comment-actions in src/actions/comment-actions.js to simulate
   comment calling from API.(GetLastComment(implemented), GetCommentById, GetCommentsByPostId etc)

3. PROPOSE idea on how to display images in posts.

4. Create-Post component ACCORDING TO CURRENT DESIGN.